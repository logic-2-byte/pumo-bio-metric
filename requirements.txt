# Biometric capture service — runtime dependencies.

# --- talking to the readers -------------------------------------------
# Nothing. Readers are given this service's address and push to the ADMS /
# iClock HTTP routes, so the web layer below IS the device protocol.
#
# `pyzk` (the TCP 4370 socket client) went with the outbound poller: a reader
# sits on a branch LAN behind a router this service cannot route to, so dialling
# it failed on healthy devices and cost egress to establish nothing. The only
# remaining user is tools/list_device_admins.py, a hands-on diagnostic run from
# inside the branch network — `pip install pyzk` by hand if you need it.

# --- talking to the LMS -----------------------------------------------
psycopg2-binary>=2.9.10

# --- the web layer: dashboard + ADMS/iClock listener -------------------
fastapi>=0.115.0
starlette>=0.40.0
uvicorn>=0.30.0
gunicorn>=23.0.0
python-multipart>=0.0.9
pydantic>=2.10.0
pydantic-settings>=2.8.0
pydantic-core>=2.27.0
python-dotenv>=1.0.0

# --- logging -----------------------------------------------------------
loguru>=0.7.2

# --- dependencies & utilities -----------------------------------------
annotated-types>=0.7.0
anyio>=4.6.0
certifi>=2024.8.30
click>=8.1.7
h11>=0.14.0
idna>=3.8
sniffio>=1.3.1
typing-inspection>=0.4.0
typing_extensions>=4.12.2
watchfiles>=0.24.0
websockets>=13.0
